# conversiontools

Convert files between 140+ formats using the ConversionTools MCP server. Use when the user needs to convert documents (Word, PDF, Excel, PowerPoint), data formats (JSON, CSV, XML, YAML), images (PNG, JPG, WebP, AVIF, HEIC, SVG), audio, video, e-books, OCR text extraction, AI-powered data extraction, subtitle conversion, or website screenshots.

## Installation

### Claude Code

```bash
claude plugin marketplace add conversiontools/agent-skills
claude plugin install conversiontools@conversiontools-skills
```

### npx skills

```bash
npx skills add conversiontools/agent-skills --skill conversiontools
```

---

> This plugin is auto-generated from [skills/conversiontools](../../skills/conversiontools).
